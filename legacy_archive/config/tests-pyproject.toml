[project]
name = "test-lib"
version = "0.1.0"
description = "Here we will be test our library!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyRunOF"]

[tool.uv.sources]

pyRunOF = {path = "../pyRunOF"}

